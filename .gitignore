*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
app/build/
.kotlin

# ML training artifacts (datasets, venv, checkpoints) — keep source only
ml/data/
ml/.venv/
ml/runs/
__pycache__/

# Session dumps & scratch
context

# Signing & Keystores
*.keystore
*.jks
# ...and any suffixed variant (e.g. release.keystore.orphaned-YYYYMMDD from a
# key rotation). Key material must never reach git, whatever it is named.
*.keystore.*
*.jks.*
keystore.properties
app/keystore.properties
